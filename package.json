{
  "name": "pesto-ui",
  "version": "0.0.0",
  "workspaces": [
    "packages/**/*",
    "services/**/*"
  ],
  "scripts": {
    "build": "nx build",
    "graph": "nx run-many --target=build --graph"
  },
  "packageManager": "yarn@4.5.3",
  "dependencies": {
    "@pesto-ui/themes": "workspace:^",
    "dev": "^0.1.3",
    "nx": "^20.1.4",
    "typescript": "^5.6.3"
  }
}
